<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Do Not Waste - Food Platform</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-success mb-4">
        <div class="container">
            <a class="navbar-brand" href="#">Do Not Waste Platform</a>
            <% if (typeof user !== 'undefined') { %>
                <span class="navbar-text text-white">Welcome, <%= user.name %> (<%= user.userType %>) | <a href="/logout" class="text-white">Logout</a></span>
            <% } %>
        </div>
    </nav>
    <div class="container">